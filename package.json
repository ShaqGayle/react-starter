{
  "name": "react-starter",
  "version": "0.0.1",
  "description": "My attempt at making a starter for myself.",
  "main": "index.js",
  "repository": "https://github.com/ShaqGayle/react-starter.git",
  "author": "Shaquille Gayle",
  "license": "MIT",
  "scripts": {
    "pre-commit": "cd api && npm run pre-commit && cd ../client && npm run pre-commit",
    "install-dependencies": "npm install && cd api && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "husky": "^4.2.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run pre-commit"
    }
  }
}
